import React from 'react';

const SlideShow = () => {
    // const slideStyle = {
    //     background: '#f0f0f0',
    //     padding: '10px',
    //     borderRadius: '5px',
    //     textAlign: 'center',
    //     marginBottom: '10px',
    // };

    return (
        <div style={{ marginTop: '20px' }}>
            {/* {Array(7).fill("srimandir devotees").map((text, index) => (
                <p key={index} style={slideStyle}>
                    {text}
                </p>
            ))} */}
        </div>
    );
}

export default SlideShow;
